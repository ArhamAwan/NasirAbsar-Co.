# Exclude PHP files from Vercel deployment
# PHP files should only be on cPanel, not Vercel
*.php
**/*.php

# Exclude API directory (PHP files)
api/
**/api/

# Exclude data directory (JSON files for PHP API)
data/
**/data/

# Exclude .htaccess (only needed on cPanel)
.htaccess
**/.htaccess

